<!DOCTYPE html>
<main class="container mx-auto px-6 py-8">
    <!-- <h1 class="text-3xl font-bold text-gray-800 mb-6">Configurações</h1> -->
    <div class="bg-white shadow-md rounded-lg p-6 lg:max-w-3xl mx-auto">
        <h2 class="text-2xl font-bold text-gray-800 mb-4">Informações da Conta</h2>
        <div class="mb-4">
            <p class="text-gray-700"><strong>Nome:</strong> <%= user.fname %> <%= user.lname %></p>
        </div>
        <div class="mb-4">
            <p class="text-gray-700"><strong>Email:</strong> <%= user.email %></p>
        </div>
        <div class="mb-4">
            <p class="text-gray-700"><strong>Plano:</strong> <%= user.tier.toUpperCase() %></p>
        </div>
        <div class="mb-6">
            <button class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">
                Atualizar/Mudar seu plano
            </button>
        </div>
        <div class="mb-4">
            <form action="/logout">
                <button id="logoutBtn" type="submit" class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
                    <i class="fas fa-sign-out-alt mr-2"></i>Sair desta conta
                </button>
            </form>
        </div>
    </div>
</main>